;;; autocomplete.init.el -- Autocomplete config

;;; Commentary:
;;; This is currently bare, as we only ensure that
;;; autocomplete is loaded

;;; Code:

(use-package auto-complete-config
  :config
  (ac-config-default))

;;; autocomplete.init.el ends here
